# Airflow environment variables
# Copy this to .env in your Astro project

# Enable connection testing in Airflow UI
AIRFLOW__CORE__TEST_CONNECTION=Enabled

# Note: never commit real passwords to Git
# Store credentials in Airflow Connections UI instead
